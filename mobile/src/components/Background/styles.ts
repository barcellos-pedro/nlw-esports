import { StyleSheet } from 'react-native';
import { THEME } from '../../theme';

export const styles = StyleSheet.create({
  container: {
    flex: 1, // Fills whole screen (height and width)
    backgroundColor: THEME.COLORS.BACKGROUND_800,
  },
});
